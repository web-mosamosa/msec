<%= render 'admin/admin_header' %>
<div class="col-md-8 col-md-offset-2">
<h2>商品 詳細画面</h2>
	<h3>商品<%= @item.name %></h3>
    <h3>商品説明<%= @item.text %></h3>
    <h3>ジャンル<%= @item.category.name %></h3>
    <h3>税込価格（税抜価格）<%= @item.price %>(<%= @item.price %>)円</h3>
    <h3>販売ステータス<%= @item.sell_status %></h3>
    <%= attachment_image_tag @item, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
    <%= link_to "編集する",edit_admin_item_path(@item), class: "btn btn-primary" %>
</div>